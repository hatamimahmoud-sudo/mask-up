{
  "name": "mask-up",
  "private": true,
  "description": "Live face masks in the browser. The app itself is a single file with no build step. This manifest only covers the preview tool.",
  "scripts": {
    "preview": "node tools/preview.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
